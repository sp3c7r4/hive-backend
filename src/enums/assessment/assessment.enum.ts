export enum QuizQuestionType {
	MULTIPLE = "multiple",
	TRUEFALSE = "truefalse",
	FILLBLANK = "fillblank",
}

export enum AssignmentSubmissionStatus {
	PENDING = "pending",
	SUBMITTED = "submitted",
	GRADED = "graded",
	RETURNED = "returned",
}
