export enum UserStatus {
	ACTIVE = "active",
	INACTIVE = "inactive",
	SUSPENDED = "suspended",
	DELETED = "deleted",
}

export enum BusinessStatus {
	BLOCKED = "blocked",
	ACTIVE = "active",
}

export enum WorkerStatus {
  PROCESSING = "processing"
}
